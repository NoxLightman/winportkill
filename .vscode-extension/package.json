{
  "name": "winportkill-vscode",
  "displayName": "WinPortKill",
  "description": "Inspect local Windows x64 listening ports and processes inside VS Code.",
  "version": "0.1.0",
  "publisher": "noxlightman",
  "license": "MIT",
  "homepage": "https://github.com/NoxLightman/winport-killer",
  "bugs": {
    "url": "https://github.com/NoxLightman/winport-killer/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NoxLightman/winport-killer.git"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "ports",
    "process",
    "windows",
    "developer-tools",
    "network",
    "kill-port"
  ],
  "icon": "media/icon.png",
  "main": "./dist/extension.js",
  "activationEvents": [
    "onView:winportkill.sidebar",
    "onCommand:winportkill.refresh",
    "onCommand:winportkill.killSelected"
  ],
  "extensionKind": [
    "ui"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "winportkill",
          "title": "WinPortKill",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "winportkill": [
        {
          "type": "webview",
          "id": "winportkill.sidebar",
          "name": "Inspector"
        }
      ]
    },
    "commands": [
      {
        "command": "winportkill.refresh",
        "title": "WinPortKill: Refresh"
      },
      {
        "command": "winportkill.killSelected",
        "title": "WinPortKill: Kill Selected"
      }
    ],
    "configuration": {
      "title": "WinPortKill",
      "properties": {
        "winportkill.refreshIntervalSeconds": {
          "type": "number",
          "default": 10,
          "minimum": 3,
          "description": "Refresh interval for the WinPortKill panel."
        },
        "winportkill.sidecarPath": {
          "type": "string",
          "default": "",
          "description": "Optional absolute path to a local winportkill.exe sidecar. Leave empty to use the cached GitHub Releases download."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.100.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.8.3"
  }
}
